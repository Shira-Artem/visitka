/* ============================================================
   cat-journey/config.js — единственный источник длины и раскладки
   сценария (по образцу story/config.js: числа тут, а не разбросаны
   по index.js).
   ============================================================ */

// Итоговая длина закреплённой сцены в svh — бриф просил 300–350svh.
export const TOTAL_VH = 320;

// Доля таймлайна (0–1, в сумме 1) на каждый акт — по ней же делится
// «огненная линия»/точка маршрута, поэтому веса и переходы совпадают.
export const ACTS = [
  { id: 1, weight: 0.32 },
  { id: 2, weight: 0.34 },
  { id: 3, weight: 0.34 },
];
